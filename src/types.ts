export interface ComboboxIds {
  stateComboboxId: string;
  rtoComboboxId: string;
  yaxisComboboxId: string;
  xaxisComboboxId: string;
  selectedYearCombobxId: string;
}

export interface Items {
  id: string;
  value: string;
}
